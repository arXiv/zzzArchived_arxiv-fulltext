language: python
cache: pip
sudo: required
services:
  - docker
os:
  - linux
python:
  - "3.6"
script:
  - $(aws ecr get-login --no-include-email --region us-east-1)
  - pip install -r requirements/test.txt
  - nose2 -s extractor
  - nose2
after_success:
  - coveralls
