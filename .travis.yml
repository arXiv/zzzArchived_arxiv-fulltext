language: python
cache: pip
sudo: required
services:
  - docker
os:
  - linux
python:
  - "3.6"
script:
  - pip install -r requirements/test.txt
  - nose2
  - nose2 -s extractor
after_success:
  - coveralls
